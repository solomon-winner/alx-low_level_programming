#include<stdio.h>
int main(void)
{/**
   *this is the main function that is the Entry point
   * 
   *it will always return 0 that is success
   *
   */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
